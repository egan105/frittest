<!DOCTYPE html>
<html>
  <head>
    <title>Fritter - Sign up</title>
    <!--Credit to Twitter Bootstrap and Font Awesome-->
    <%- include ../header.ejs %>
  </head>
  <body>
    <div class="container">
      <div class="col-sm-6 col-sm-offset-3">
        <div class="well text-center">
          <h1>Sign Up for Fritter</h1>
        </div><hr>
        <% if (message != '') { %>
          <div class="alert alert-danger"><%= message %></div>
        <% } %>

        <form name="input" action="create" method="post">
          <div class="form-group">
            <label>Username</label>
            <input type="text" class="form-control" name="user">
          </div>
          <div class="form-group">
            <label>Password</label>
            <input type="password" class="form-control" name="user_pw">
          </div>

          <button type="submit" value="Submit" class="btn btn-info btn-lg">Sign Up</button>
        </form>

      <hr>
      <p>Go back <a href="/">Home</a>.</p>

    </div>

  </div>
  </body>
</html>
